{
  "name": "hello-cli",
  "version": "1.0.0",
  "description": "hello cli with authentication and additional features.deep diving into cli ",
  "main": "bin/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "harish reddy m",
  "license": "ISC",
  "bin": {
    "hello": "./bin/index.js"
  },
  "dependencies": {
    "chalk": "^3.0.0",
    "figlet": "^1.2.4"
  }
}
